---
title: ML Atlas
description: Machine learning and AI research concepts explained from first principles through to the research frontier.
template: splash
hero:
  tagline: One concept per page. Four depth tiers per concept. Start at zero, finish at the frontier.
  # NOTE: hero action links are frontmatter, so they are neither JSX nor part of
  # the Markdown AST — Starlight leaves them exactly as written and the
  # rehype-base-links plugin never sees them. They must include the base path
  # (/notes) literally. If you change `base` in astro.config.mjs, change these
  # by hand too; they are the only links in the project that don't follow
  # automatically.
  actions:
    - text: Reading order
      link: /notes/curriculum/
      icon: right-arrow
      variant: primary
    - text: How to read a page
      link: /notes/how-to-read/
      icon: open-book
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid>
  <Card title="1 · Foundations" icon="seti:notebook">
    Backpropagation, probability, tokenization, representations and latent
    spaces, generalization, evaluation, interpretability, causality. Everything
    else assumes the first two. **6 h**
  </Card>
  <Card title="2 · Encoders" icon="seti:plan">
    What an encoder is in all three senses — neural, architectural, and
    preprocessing — with a page for each. **2.3 h**
  </Card>
  <Card title="3 · Architectures" icon="puzzle">
    Transformers, state-space models, mixture of experts, the generative model
    zoo, diffusion, energy-based models, JEPA, multimodal. **5.8 h**
  </Card>
  <Card title="4 · Embeddings" icon="magnifier">
    word2vec through instruction-tuned LLM embedders, plus quantization, ANN
    index internals, and the operational failure modes. **2.5 h**
  </Card>
  <Card title="5 · Training & Objectives" icon="setting">
    Optimization, self-supervision, fine-tuning and PEFT, post-training and
    alignment, RL, in-context learning, AI safety. **4.3 h**
  </Card>
  <Card title="6 · World Models & Planning" icon="rocket">
    Learned simulators, latent dynamics, model-predictive control, hierarchy,
    and a catalogue of every variant. **2.9 h**
  </Card>
  <Card title="7 · Domains" icon="star">
    Where the general machinery meets a specific structure: graphs, time series,
    speech and audio. **1.7 h**
  </Card>
  <Card title="8 · Systems" icon="random">
    Distributed training, inference and serving, retrieval and RAG, agents and
    tool use. **2.4 h**
  </Card>
  <Card title="9 · Hardware & Compute" icon="laptop">
    What hardware you need and how much compute a job costs, answered with
    arithmetic rather than folklore. **2.1 h**
  </Card>
</CardGrid>

## The sidebar is a curriculum

Sections are numbered in prerequisite order — **nothing in a section depends on a
section below it** — and pages within a section follow the same rule. Read top to
bottom and no term arrives before it has been introduced.

This is enforced rather than intended: `npm run check:order` fails the build if
any page lists a prerequisite from a later section. If you would rather not read
all thirty hours, the [reading order](/curriculum/) page has three shorter tracks
— new to the field, heading for research, and building a system.

## What this is

A reference for ML and AI research concepts where every page is written to be
read by someone who has never heard the term *and* by someone who works on it.
That is not a compromise between the two — it is a **layered** page. Tier 1 has no
maths at all. Tier 4 assumes you have read the papers.

You can stop at any tier. The page is designed so that stopping early still
leaves you with something correct, just less complete.

## What this is not

- **Not a tutorial series.** There is no code you run to build a thing. It
  explains; it does not walk you through an implementation.
- **Not comprehensive.** Coverage is deliberately narrow and deep. A page exists
  only when there is something to say beyond what a definition would give you.
- **Not neutral where the evidence is not.** Where a question is genuinely open,
  pages say so and give both sides. Where one position is better supported, pages
  say that instead of hedging.
